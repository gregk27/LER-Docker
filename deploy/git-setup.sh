#!/bin/bash

# Configure git for team use
git config --global user.name "LER-Programming"
git config --global user.email "github.lakeeffectrobotics@gmail.com"
git config --global core.hooksPath /ler/git-hooks